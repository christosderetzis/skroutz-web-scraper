
spring:
  application:
    name: skroutz-web-scraper
  
  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5432}/skroutz_scraper
    username: skroutz_user
    password: skroutz_password
    driver-class-name: org.postgresql.Driver
  
  # JPA/Hibernate Configuration
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  
  # Flyway Configuration
  flyway:
    schemas: scraper_schema

  # Elasticsearch Configuration
  elasticsearch:
    uris: http://localhost:${ELASTICSEARCH_PORT:9200}

# Scraper Configuration
scraper:

  base-url: https://www.skroutz.gr
  timeout-seconds: 30
  specifications:
    batch-size: 30

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
server:
  port: 8082